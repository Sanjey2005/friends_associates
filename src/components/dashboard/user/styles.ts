import type React from 'react';

export const userSectionTitle: React.CSSProperties = {
    fontFamily: 'var(--font-serif)',
    fontSize: 'clamp(1.75rem, 3.5vw, 2.25rem)',
    fontWeight: 500,
    color: 'var(--color-text)',
    lineHeight: 1.15,
    letterSpacing: '-0.01em',
    marginBottom: '1.5rem',
    display: 'flex',
    alignItems: 'center',
    gap: '0.625rem',
};
